#!/usr/bin/env bash

session=$( (echo new; tmux list-sessions -F '#S') | fzf --ansi --reverse --height=25%)

if [[ "new" = "${session}" ]]; then
    tmux new -s main
elif [[ -z "${session}" ]]; then
    echo "Cancel"
else
    tmux attach -t "${session}"
fi
